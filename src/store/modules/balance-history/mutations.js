export default {
    SET_BALANCE_HISTORIES: (state, balanceHistories) => {
        state.balanceHistories = balanceHistories
    },

    SET_PAGINATION: (state, pagination) => {
        state.pagination = pagination;
    }
}
